# @uvl/ts-client

Generated TypeScript client for the UVL REST API.

**Status:** stub. Generated once `../../schemas/openapi.yaml` is populated.

## Regenerate

```bash
cd frontend
npm run types:generate
```

The types flow into `frontend/src/api/generated/` and a thin wrapper under
this directory re-exports them as an installable npm package.
