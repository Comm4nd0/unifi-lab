# uvl-client (Python)

Generated Python client for the UVL REST API.

**Status:** stub. Generated once `../../schemas/openapi.yaml` is populated.

## Regenerate

```bash
cd clients/python
uv run python -m uvl_client.build
```

The build step invokes `openapi-python-client` against the checked-in
schema snapshot.
